namespace OpenBaseCamp.App.ViewModels;

/// <summary>A built-in icon as offered in the inspector's icon picker.</summary>
public sealed record IconDescriptor(string Id, string Name, string Category);
